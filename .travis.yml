language: ruby
cache: bundler

rvm:
  - 2.2.0

before_script: 'bundle install'
script: 'bundle exec cucumber'

branches:
  only:
    - master

notifications:
  email:
    - iamsmj@gmail.com

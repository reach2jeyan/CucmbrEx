source 'https://rubygems.org'

language: ruby
cache: bundler

rvm: 2.2.0

before_script:
        - bundle install
        - gem install watir
script: bundle exec cucumber

branches:
  only:
    - master

notifications:
  email:
    - iamsmj@gmail.com
